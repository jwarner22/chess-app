import React from 'react';
import styled from 'styled-components'

const FooterBuffer = () => {
  return <FooterContainer />;
};

export default FooterBuffer;

const FooterContainer = styled.div`
    width: 100%;
    height: 400px;
    background: #EEF0FF;
`